apiVersion: apps/v1
kind: Deployment
metadata:
  name: echoserver-vpc-deployment
spec:
  replicas: 5
  selector:
    matchLabels:
      app: echoserver-vpc-app
  template:
    metadata:
      labels:
        app: echoserver-vpc-app
    spec:
      containers:
      - image: ealen/echo-server:latest
        imagePullPolicy: IfNotPresent
        name: echoserver
        ports:
        - containerPort: 80
        env:
        - name: PORT
          value: "80"